# 项目常见问题

本文档记录项目级别的常见问题及其解决方案。

---

*最后更新: 2026-01-24*

## Hook 编写中的通用性问题

### 问题描述

通用性的 Claude Code UserPromptSubmit hook 中包含了项目特定对象，导致其他项目无法复用该 hook。

### 触发场景

- 编写跨项目可复用的 hook 时
- 在 hook 的正则表达式规则中包含特定项目的业务对象名称

### 根本原因

- Hook 规则中硬编码了特定项目的对象名称（如 "热力图"、"heatmap"、"页面"、"数据"、"图表"）
- 使用类似 `/(没有|缺失).*?(热力图|heatmap|范围|数据|图表)/i` 的正则表达式
- 这些特定对象限制了 hook 在其他项目中的适用性

### 解决步骤

1. **识别项目特定对象**
   - 审查 hook 中的所有正则表达式模式
   - 找出包含业务对象名称的规则

2. **替换为通用模式**
   ```javascript
   // ❌ 错误：包含项目特定对象
   descriptive: [/(没有|缺失).*?(热力图|heatmap|范围|数据|图表)/i]
   positive: [/^(优化|改进).*?(ui|界面|组件|热力图|heatmap|样式|页面|布局)/i]

   // ✅ 正确：使用通用模式
   descriptive: [/(没有|缺失|不显示|无法显示|找不到|看不到|不存在|出错了|有问题)/i]
   positive: [/^(优化|改进|调整|修改|设计|重新设计|更新)/i, /^(参考|模仿|借鉴)/i]
   ```

3. **使用通用动词和模式**
   - 动作动词：修复、实现、优化、设计、调整
   - 描述性词汇：没有、缺失、不显示、找不到
   - 排除查询类：查看、显示、是什么、如何

4. **测试验证**
   ```bash
   echo '{"prompt":"修复登录按钮问题"}' | node .claude/hooks/user-prompt-submit.cjs
   echo '{"prompt":"设计新页面"}' | node .claude/hooks/user-prompt-submit.cjs
   echo '{"prompt":"查看修复历史"}' | node .claude/hooks/user-prompt-submit.cjs
   ```

### 预防

- **设计原则**：Hook 应该使用通用动词和模式，不包含项目特定对象
- **代码审查**：在添加新规则时，检查是否包含业务特定术语
- **测试覆盖**：使用通用测试用例验证 hook 跨项目可用性
- **文档说明**：在 hook 代码注释中明确标注设计原则

---

*最后更新: 2026-01-24*
